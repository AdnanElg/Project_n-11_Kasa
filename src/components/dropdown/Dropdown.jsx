/* eslint-disable react/prop-types */
// import DropdownStyle from "./Dropdown.scss";

const Dropdown = ({ itemData }) => {
  console.log(itemData);
  return <section></section>;
};

export default Dropdown;
